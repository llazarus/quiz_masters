<div class="jumbotron">
  <h1 class="display-4">Welcome to Quiz Masters!</h1>
  <p class="lead">Test your skills against the best in the biz! 👨🏽‍💻👩🏿‍💻</p>
  <hr class="my-4">
  <% if user_signed_in? %>
    <%= link_to("Enter, If You Dare!", quizzes_path, class: "btn btn-primary btn-lg") %>
  <% else %>
    <%= link_to("Enter, If You Dare!", new_user_session_path, class: "btn btn-primary btn-lg") %>
  <% end %>
</div>