<h1>Quizzes</h1>

<ul>
  <% @quizzes.each do |quiz| %>
    <li>
      <%= link_to(quiz.title, quiz_path(quiz.id))%>

      <%= link_to( 
        "Delete", 
        quiz_path(quiz.id), 
        method: :delete, 
        data: { confirm: "Are you sure?" })
      %>


    </li>
  <% end %>
</ul>